server:
  port: 9091
  error:
    include-message: always
    include-binding-errors: always

feign.client.config.default:
  loggerLevel: full
  connectTimeout: 5000

logging.level:
  br.com.tt.petshop.client: DEBUG

client.credito.url: https://imersao-credito-api.herokuapp.com

spring.datasource.url: ${DATABASE_URL}